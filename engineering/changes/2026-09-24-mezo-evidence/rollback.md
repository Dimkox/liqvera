# Rollback plan — Liqvera Mezo Evidence

Before any payment state exists, stop the new stack and revert the stage's
coherent commit. Do not alter the existing Stage A fixture/demo path.

After a payment attempt exists, disable new quote creation and preserve the
ledger and artifact volumes. Roll back stateless gateway/report binaries only
to a schema-compatible version. Unknown or confirmed attempts are reconciled
or forward-fixed; they are never erased, reclassified as unpaid, or retried
blindly.
